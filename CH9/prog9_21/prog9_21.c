#include<stdlib.h>
#include<stdio.h>

int main(){

    char name[15];
    puts("What's your name?");
    gets(name);
    puts("Hi");
    puts(name);
    puts("How are you?");

    system("pause");
    return 0;
}
